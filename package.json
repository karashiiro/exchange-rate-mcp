{
  "name": "exchange-rate-mcp",
  "version": "1.0.0",
  "description": "MCP server providing exchange rate data from Norges Bank API",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "ts-node src/index.ts",
    "format": "prettier --write src test",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.8.0",
    "node-fetch": "^3.3.0",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@vitest/coverage-v8": "^3.1.1",
    "prettier": "^3.5.3",
    "ts-node": "^10.9.0",
    "typescript": "^5.0.0",
    "vitest": "^3.1.1"
  }
}
